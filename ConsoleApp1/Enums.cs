namespace GameAccount
{
    public enum GameAccountType
    {
        Normal,
        LoseStreak,
        WinStreak
    }
    public enum GameType
    {
        Normal,
        Ranked
    }
    public enum  GameResult
    {
        Win,
        Lose
    }
}
